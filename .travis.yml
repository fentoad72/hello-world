language: python

python: 
 - 2.7
 - 3.6

install:
  - pip install pylint

jobs:
  - allow_failure:
    - linting

scripts:
  - name: prelim
     - pwd
     - which pylint
     - ls -l
  - name: helloworld
     - ./hello_world.py
  - name: linting
     - pylint ./hello_world.py

#script: 
#  - prelim
#  - hello
#  - linting

#after_script:

#
branches:
        only:
                - master
                - travis
