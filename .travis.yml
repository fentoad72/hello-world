language: python
os: linux
dist: xenial
version: ~> 1.0

python: "2.7"
#python: 
#  - "2.7"
#  - "3.6"

install:
  - pip install pylint

#install: skip

jobs:
  - include:
    - stage: prelim
      script: 
        - pwd
#       - which pylint
        - ls -l
    - stage: run_hello
      script: ./hello_world.py

after_script:
  - pylint ./hello_world.py
# - allow_failure: pylint


#
branches:
        only:
                - master
                - travis
